<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinApp.Views.StartPage" BackgroundColor="{StaticResource BackgroundColor}">
    <ContentPage.Content>

        <StackLayout Orientation="Vertical" Padding="30,24,30,24" Spacing="10">
            <Label Text="TrackMyMoney" 
               FontSize="Title" 
               TextColor="{StaticResource Primary}" 
               FontAttributes="Bold"
                HorizontalTextAlignment="Center"
            VerticalTextAlignment="Center" />
            <Label Text="Here you can sign in your account. If you do not have one yet, register and start tracking your money!" FontSize="16" Padding="0,0,0,0" TextColor="{StaticResource Primary}" />
            <StackLayout Padding="10,0,10,0" VerticalOptions="Center" Orientation="Horizontal" Margin="0,100,0,0">
                <Button VerticalOptions="Center" Text="Login" Clicked="OnLoginClicked" HorizontalOptions="FillAndExpand"/>
                <Button VerticalOptions="Center" Text="Register" Clicked="OnRegisterClicked" HorizontalOptions="FillAndExpand"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>